---
  home:
    devices:
      - !secret device_user_one
      - !secret device_user_two
    tolerance: 50
  home_user_one:
    devices:
      - !secret device_user_one
    tolerance: 50
  work:
    zone: Work
    devices:
      - !secret device_user_one
    tolerance: 50
